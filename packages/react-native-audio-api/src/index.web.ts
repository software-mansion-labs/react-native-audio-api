const AudioContext = window.AudioContext;

export { AudioContext };

export type {
  OscillatorType,
  BiquadFilterType,
  ChannelCountMode,
  ChannelInterpretation,
  ContextState,
} from './core/types';
